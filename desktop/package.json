{
  "name": "kevfin-desktop",
  "version": "1.1.0",
  "private": true,
  "description": "KevFin desktop app — an Electron wrapper around the local server + client.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "stage": "node scripts/stage.js",
    "dist": "electron-builder --config electron-builder.yml"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.4",
    "electron": "^33.0.0",
    "electron-builder": "^26.15.3"
  }
}
